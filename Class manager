Struct mode_manager
{
    uint32_t curr_mode;
    Uint32_t nxt_mode;
    Std:mutex m;
    
    class enum mode_states
    {
        OFF;
        STANDBY;
        READY;
        ACTION1;
        ACTION2;
        ACTIONN;
        RESET;
    };

    class enum mode_status
    {
        SUCCESS;
        FAIL;
    }

    mode_status modeManager(mode_states input1)
    {
        Switch( input1 )
        {
             case STANDBY:
             
             break;
             
             case READY:
             break;

             case ACTION1:
             break;
             
             case ACTION2:
             break;

             default:
             break;

        }
        
        mode_status standby_mode()
        {
             mode_status reset_stat=reset();
             mode_status bit_stat=builtin_test();
             if( reset_stat != SUCCESS)
             {
                //power_cycle?
             }
             else if(bit_stat != SUCCESS)
             {
               //report back BIT results?
             }
             return SUCCESS;
             
        
        }
        
    
    }
    
    


}
